group :development, :test do
  # Call 'debugger' anywhere in the code to stop execution and get a debugger console
  #gem 'debugger'

  gem 'byebug'
end

source "https://rubygems.org" do
	gem 'kramdown'
	gem 'jekyll-sitemap'
	gem 'nokogiri'
	gem 'syncfusion-tabs'
	gem 'syncfusion-tabs-id'
end
